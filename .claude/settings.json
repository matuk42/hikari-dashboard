{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "git add . && (git diff --cached --quiet || (git commit -m \"auto: save changes\" && git push)) || exit 0",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
